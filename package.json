{
  "name": "electron-todo",
  "version": "1.0.0",
  "description": "electron to do list app",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run compile && electron .",
    "compile": "tsc && copyfiles -f index.html dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "pius gachaga",
  "license": "ISC",
  "devDependencies": {
    "electron": "^29.0.0"
  },
  "dependencies": {
    "copyfiles": "^2.4.1",
    "path": "^0.12.7",
    "typescript": "^5.3.3"
  }
}
